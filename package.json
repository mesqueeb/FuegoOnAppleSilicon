{
  "name": "FuegoOnAppleSilicon",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "bump": "np --no-cleanup --no-tests --no-publish",
    "release": "npm run zip-framework && npm run bump",
    "zip-framework": "node ./node/zipFramework.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "np": "^10.0.5",
    "semver": "^7.6.2"
  }
}
